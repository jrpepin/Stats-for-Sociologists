---
title: "Milestone 01"
author: "FirstName LastName"
format: html
---

# Environment
Update the YAML with your first and last name.

Load the packages from the code-along. (Install it not already done so.)

```{r}
#| label: load

library(here)
library(tidyverse)
library(gssr)
library(gssrdoc)
```


# Add Documentation
Run and add narrative (outside code chunks) or comments (inside the code chunks)
to the R chunks below.

```{r}
#| label: here

here()
```


```{r}
#| label: list-files

list.files(path = here())

list.files(path = here("project"))
```


```{r}
#| label: time

Sys.time()
```

