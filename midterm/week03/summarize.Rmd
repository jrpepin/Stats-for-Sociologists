Question
========
What does the following code return?  

```{r output, eval = FALSE, results = "asis"}
#| message: false

gss24 |> 
  group_by(sex) |> 
  summarize(mean_age = mean(age, na.rm = TRUE))

```

Answerlist
----------

-   A data frame showing the average age for each sex.

-   A list of all ages grouped by sex.

-   A filtered data frame with only rows where age is not missing.

-   A new column named `mean_age` added to the original data frame.

Meta-information
================
exname: summarize 
extype: schoice 
exsolution: 1000 
exshuffle: 4
