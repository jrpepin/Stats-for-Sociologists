---
title: "Questions"
format: html
---

-   *What happens if you try to use a function from a package without
    loading it?*

<!-- -->

-   *You get an error saying “could not find function.” What might be
    the cause?*

-   *How can you check which package a function comes from?*

    -   use `find()`; use `help()` or ?function (if the package is
        loaded)

-   *Can you name a function from the* `dplyr` *package? How is it
    different from a base R function?*

<!-- -->

-   *What does* `::` *do in R, and when would you use it?*

-   *Why is it important to know which package a function comes from
    when writing reproducible code?*

<!-- -->

-   *How would you explain to someone new to R why packages are useful?*
